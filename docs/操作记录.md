# 操作记录

> **维护对象**：任何 AI 助手和人类读者。
> **目的**：记录每一次对代码或文档有实际变更的事项，防止上下文在长会话或跨会话中丢失。

## 维护规范

1. **每个条目必须有状态**：🟢 已完成 / 🟡 进行中 / 🔴 废弃
2. **废弃条目处理**：确认无历史参考价值后删除；有保留价值的（如设计方向曾被尝试后放弃）改为废弃态并注明原因
3. **新条目追加**：按时间顺序，新增在同 Phase 节末尾或创建新节
4. **内容底线**：做什么 + 为什么 + 结果。不记录纯讨论/调研，只记录对代码或文档产生了实际变更的事项
5. **时效性**：进行中的条目超过 7 天无更新，主动询问是否仍有效或标记废弃

## 2026-06-09

### 🟢 Android 审查修复批次 A1-A6

- **做什么**：按 `docs/android_code_review.md` 的审查计划，修复首屏初始化竞态、`teamNo/personNo` 混用、自动标记被误升级、恢复默认工时空字段、后台提醒旧 API、月度逐日串行聚合等问题。
- **为什么**：这些问题会直接造成每日/月度数据不同步、标记写错命名空间、提醒数据源和前台数据源分裂、月度加载慢且错误不可诊断。
- **结果**：新增 `StartupRefreshCoordinator`、`CalendarMarkMerge`、`ReminderAttendanceFetcher`、`MonthlyAttendanceAggregator` 及对应测试；每日页、月度页、提醒服务开始复用统一工具与 API 入口。

### 🟢 Android 审查修复批次 A7-A11

- **做什么**：继续修复 API 客户端吞错、设置 schema 新旧字段并存、提醒设置 key 分裂、多班次考勤解析、自动请假误判来源不明空数据等问题。
- **为什么**：这些问题会让真实业务错误被压成 `null/false`，让旧缓存或默认空数据被误认为 API 已确认事实，也会在多班次或跨天下班场景丢失考勤数据。
- **结果**：`HikiotApiClient` 改为保留异常语义并支持注入测试客户端；`StorageService` 统一设置和提醒 key 迁移；`AttendanceParser` 支持全部 `shiftDetails` 与凌晨跨天；`SmartDayTypeHelper` 新增 `DayDataSourceStatus`，只在 API 明确确认空打卡时自动判请假。
- **验证**：批次完成时 `D:\Environments\flutter\bin\flutter.bat test` 当前 30/30 通过；后续静态分析清理见下一条。

### 🟢 Android 静态分析基线清理

- **做什么**：按 `AGENTS.md` 的维护要求继续优化剩余内容，清理已触碰 Android/Dart 文件中的 `flutter analyze` warning/info，并同步 `docs/android_code_review.md`。
- **为什么**：审查报告里记录的 analyze 剩余问题已经过时；继续开发前需要把静态分析基线压到 clean，避免后续真实问题淹没在旧噪音里。
- **结果**：修复未使用 import、废弃 `Switch.activeColor`、构造函数 super 参数、多余 `toList()`、不明确的异步 `BuildContext` 使用等问题；每日编辑弹窗的保存/恢复回调改为等待异步落盘后再关闭。
- **验证**：`D:\Environments\flutter\bin\flutter.bat test` 当前 30/30 通过；`D:\Environments\flutter\bin\flutter.bat analyze` 当前无问题；`git diff --check` 当前无空白错误。

### 🟢 Android debug APK 测试包构建

- **做什么**：为手机测试构建 Android debug APK。
- **为什么**：`flutter build apk --debug` 在当前 PowerShell 会话先遇到 `JAVA_HOME` 缺失，补齐环境后又因 Flutter 外层命令超时留下不可观察的 Gradle/Kotlin 进程；改用 Gradle wrapper 直接执行 debug 构建，便于定位和规避卡住的外层包装进程。
- **结果**：使用 `.\gradlew.bat --no-daemon --console=plain --info -Dkotlin.compiler.execution.strategy=in-process --max-workers=4 :app:assembleDebug` 成功生成 `hikiot_worktime/build/app/outputs/flutter-apk/app-debug.apk`。
- **验证**：`apksigner verify --print-certs` 通过，签名为 Android Debug 证书；APK 产物大小约 160 MB。

## 2026-06-10

### 🟢 Android 审查修复批次 A9 页面层存储收口

- **做什么**：继续修复 `docs/android_code_review.md` 中 A9 的存储魔法字符串问题，把启动页、登录页、每日页、免责声明弹窗、设置页和 token 失效服务中的 token、onboarding、免责声明、调试开关、扩展目标、团队展示信息读写收敛到 `StorageService`。
- **为什么**：页面层直接读写 `SharedPreferences` 会让 `hikiot_token`、`disclaimer_shown`、`current_team_name` 等 key 分散失控，后续改登录态、重置引导或团队切换时容易出现“一个页面改了、另一个页面没同步”的问题。
- **结果**：`StorageKeys` 新增 `teamName`、`extendedTargetRange`、`debugToolsEnabled`；`StorageService` 新增 onboarding、免责声明、调试工具、扩展目标、团队名等统一读写方法，并兼容旧 `disclaimer_shown/current_team_*` key；月度初始化会同步保存团队名和用户名。
- **验证**：先观察新增 `storage_service_test` 失败，再实现最小代码；最终 `D:\Environments\flutter\bin\flutter.bat test` 当前 35/35 通过，`D:\Environments\flutter\bin\flutter.bat analyze` 当前无问题。

### 🟢 Android 审查修复收尾批次 A12/A13

- **做什么**：继续核对审查报告中剩余的“大页面职责过重”和“工具链升级阻塞”两项，先以当前代码和测试为准修正实际缺口。
- **为什么**：报告里已有部分抽象文件，但需要确认是否已经真正接入页面；同时当前全量验证暴露 `SettingsRepository` 少了 token 调试读写入口、`MainActivity.kt` 存在尾随空白。
- **结果**：补齐 `SettingsRepository.loadToken/saveTokenForDebug`；每日页保存/恢复手动标记改走 `DailyAttendanceRepository`；月度页日期设置保存/恢复默认改走 `MonthlyAttendanceRepository`；设置页 token 调试动作改走 `SettingsRepository`；清理 `MainActivity.kt` 尾随空白；确认 Gradle/AGP/Kotlin 已升级到 Gradle 8.14.3 / AGP 8.11.1 / Kotlin 2.2.20 并可构建。
- **验证**：新增 repository 测试先红后绿；最终 `D:\Environments\flutter\bin\flutter.bat test` 67/67 通过，`D:\Environments\flutter\bin\flutter.bat analyze` 无问题，`D:\Environments\flutter\bin\flutter.bat build apk --debug` 通过，`git diff --check` 无空白错误。

### 🟢 提交到 GitHub main 分支

- **做什么**：按用户要求将当前 Android-only 清理、审查修复、测试补齐、文档和操作记录提交到 `https://github.com/BR184/huayun_worktime` 的 `main` 分支。
- **为什么**：当前审查修复批次已经完成主要 BUG 和设计问题收口，需要把本地成果同步到远端，方便后续测试和继续迭代。
- **结果**：提交前确认本地分支为 `main`，远端 `origin` 指向 `BR184/huayun_worktime`；同步 `AGENTS.md` 与 `CLAUDE.md` 的尾部空行差异；本条记录随同代码一起进入提交。
- **验证**：提交前执行 `flutter test` 67/67 通过，`flutter analyze` 无问题，`git diff --check` 无空白错误，仅存在 Windows 换行提示。
