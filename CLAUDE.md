# 项目核心筒

> **本文档是 AI 协作的唯一持久真相源。**
> 你（AI）在每次回复后必须检查是否有需要更新的内容，并按维护协议执行。
> 若信息过时或缺失，整个项目的上下文就会坍塌。
> 为确保兼容性，目录同时存在 AGENTS.md + CLAUDE.md，更新时你需要同时维护以确保内容一致。

## 项目描述

文档在 `docs/` 目录，是规划的权威来源。

- 当前主工程是 `hikiot_worktime/`，技术栈为 Flutter，当前交付目标为 Android-only；父仓库中的 `deprecated/` 是历史归档，不纳入当前运行链。

## 开发约定

- **解耦合**：项目保证高内聚低耦合，关键内容留下特殊注释标记，以方便AI更改项目时快速定位，减少TOKENS占用，方便扩展
- **严禁堆屎山**: 禁止非用户需求的主动堆屎山套兼容编码行为。

## 维护协议

- 每次回复前后都要判断：用户是否给出了会影响后续编码、审查、运行方式、项目边界或协作偏好的稳定信息。
- 若是稳定事实，优先更新本文档；若是用户协作偏好或跨会话规则，同时写入 Claude memory。
- 不要把短期任务状态、临时日志、一次性实验结论写进本文档。
- 本文档过时比没有文档更危险；发现与当前代码或用户说明冲突时，先核实，再更新。
- 计划文档必须和用户与 AI 拟定的最新方案同步；开发中途任何影响设计、命令、流程或验收标准的修改，都必须反向更新对应 docs/*.md，计划和实际不一致比没有计划更危险。
- 本文档是上下文核心筒，不是流水账；只记录会长期影响判断的事实、规则和命令。
- 每次操作完主动更新 `docs\操作记录.md`，这很重要！
## 安全与审查口径

- 本项目面向公司使用，不按公开服务默认威胁模型审查。
- 常规审查中，除非存在特别严重问题，否则不要把安全项作为主要结论。
- 当然，你仍需显式指出的严重安全问题。
- 审查报告应着重给出 BUG、正确性、性能、可扩展性、可维护性、测试缺口和工程元数据问题。

## 代码规范

- 注释使用中文。代码是给人看的，中文注释能更快理解意图。
- 文件编码默认 UTF-8。所有 .py / .md / .yaml / .json 等文件统一 UTF-8。
- 工时百分比计算统一先将参与统计的工时截断到一位小数，再计算并将百分比结果截断到一位小数，均不四舍五入；完成率界面显示一位小数，普通工时可显示两位小数但百分位使用浅灰中空字形提示不计入工时，日历格内工时直接显示一位小数。无法使用富文本的系统通知直接显示一位小数。


对本项目的代码，确保符合以下原则：
// KISS 原则 (保持简单原则)
- 鼓励 Yourself 编写直观、简单的解决方案
- 避免过度设计和不必要的复杂性
- 生成更易读和易维护的代码

// YAGNI 原则 (你不会需要它)
- 防止 Yourself 添加推测性的功能
- 专注于实现当前确实需要的功能
- 减少代码膨胀和维护成本

// SOLID 原则 (面向对象设计的五个基本原则)
- 单一职责原则: 每个类只负责一项具体功能
- 开放封闭原则: 对扩展开放, 对修改封闭
- 里氏替换原则: 子类必须能替换掉父类

// 个人底线原则（我的对项目代码的底线）
- 相似相溶原则：维护专门的utils类，类似功能代码，务必放到合适的工具类内
- 再一再而不再三原则：严禁重复造轮子，复杂代码两次或两次以上、简单代码出现三次或三次以上，必须放到统一的类中维护（如utils类等合适的地方）
- 维护为纲原则：不过度使用语法糖，导致代码难以理解，可维护性下降
- 拓展预留原则：采用易拓展设计，对于主题模块、过滤规则等常见的有持续性增量更新的模块，使用容易拓展的设计结构
- 其他零碎原则：如有注释需求，使用中文；如有导入，统一放到一起

## 代码认知纪律

- AI 严禁看见变量名、字段名、函数名就凭直觉推断真实作用。
- 任何审查或修改前，必须主动读定义、写入点、读取点、调用链、测试和实际数据流。
- 如果名字和行为冲突，以代码具体实现和数据库实际语义为准。

## 外部系统已知约束

- 海康人员说明：默认认证方式是账号密码；新设备首次登录需要手机号短信验证码，完成一次设备验证后可继续使用账号密码。该信息目前来自人员确认，尚未由仓库代码或正式 API 文档证明；开发前必须取得登录、短信验证码、设备信任和 token 返回的正式接口契约。
- Hikiot 登录页是单页应用（SPA）：登录成功、团队选择等均为客户端路由跳转，不触发 WebView onLoadStop；App 通过 onUpdateVisitedHistory 探测 Cookie 接管登录成功时刻，团队选择由 App 原生对话框完成（用户只选一次）。网页选择团队后是否存在可复用的已选状态（Cookie/localStorage/URL）尚未探明，属真机验证项。

## Git 协作规则

### Commit 格式

```
<类型>: <中文简述>

<详细说明（可选）>

Co-Authored-By: Claude Haiku 4.5 <noreply@anthropic.com>
```

类型：feat / fix / refactor / docs / test / chore

### .gitignore 维护

- 新增文件类型/目录时自动更新 .gitignore，不要等用户提醒

### 自动推送

- 本项目默认不另开功能分支，直接在当前 `main` 上开发、验证、提交并推送；除非用户明确要求分支或 worktree 隔离。
- 完成用户明确要求的开发、修复或审查反馈处理，并完成必要验证后，主动 commit；不需要等用户再次提醒。
- 每次 commit 后自动执行 `git push`
- 不需要等用户提醒
